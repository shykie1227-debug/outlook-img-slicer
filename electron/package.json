{
  "name": "outlook-img-slicer-electron",
  "version": "6.0.3",
  "private": true,
  "type": "commonjs",
  "description": "Electron 主进程（V6.0.3）— Outlook 长图助手",
  "main": "dist-electron/main.js",
  "scripts": {
    "build": "npx tsc",
    "build:watch": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "electron": "^30.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
