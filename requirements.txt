# Outlook 长图插入工具 V3 依赖
Pillow>=10.0.0
PySide6>=6.5.0
pywin32>=306
psd-tools>=1.10
numpy>=1.24          # psd-tools 间接依赖
PyMuPDF>=1.23        # PDF / PPT 转换后 PDF 拆分
PyInstaller>=6.0.0
UPX>=3.96  # 可选：exe 压缩（Windows 下打包可选）
