# Outlook 长图插入工具 V6 依赖
#
# 下面是运行 + 打包所需的完整依赖。另有两类「可选依赖」不在此列表，
# 因为它们各自需要系统级组件或只在特定流程里才用：
#   - pdf2image：仅 ppt_slicer.py 在 PyMuPDF/COM 都不可用时的极端兜底；
#                另外需要系统安装 poppler（Windows 上还要单独部署），故不默认引入。
#   - dulwich  ：仅 verify_source_snapshot.py 使用，用于 Windows 虚拟机构建时
#                校验源码快照与提交一致；日常运行和 pytest 都不需要。
Pillow>=10.0.0
PySide6>=6.5.0
pywin32>=306
psd-tools>=1.10
numpy>=1.24          # psd-tools 间接依赖
PyMuPDF>=1.23        # PDF / PPT 转换后 PDF 拆分
python-pptx>=1.0     # PPT/PPTX 元数据与兼容依赖
lxml>=4.9            # python-pptx XML 依赖
PyInstaller>=6.0.0
cairosvg>=2.7.0      # SVG 转 PNG 转换
svglib>=1.5.1        # SVG 转 PNG 备选方案
reportlab>=4.0       # svglib 依赖
